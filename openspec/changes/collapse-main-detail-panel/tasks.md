## 1. 折叠状态与窗口尺寸

- [x] 1.1 将左侧设备栏折叠状态重构为右侧详情栏折叠状态，并保持设备栏始终可见可操作
- [x] 1.2 收起时固定窗口为设备栏加按钮窄条宽度，展开时保持当前位置并恢复收起前宽度
- [x] 1.3 紧凑状态禁止横向缩放，展开后恢复普通窗口最小宽度和横向缩放

## 2. 绘制、标题栏与控件显隐

- [x] 2.1 将 `<<` / `>>` 绘制和命中固定到设备栏右侧独立窄条，避免与设备行及滚动条重叠
- [x] 2.2 详情栏收起时跳过右侧手绘页面，并隐藏、禁用所有详情 Qt 子控件
- [x] 2.3 为紧凑窗口绘制不重叠的标题、最小化和关闭按钮，并关闭隐藏标题栏入口的悬停与点击命中
- [x] 2.4 详情栏收起时停止本机资源采样，展开且本机页选中时恢复采样

## 3. 静态复核

- [x] 3.1 检查设备选择、分组、拖拽、滚动和右键逻辑不再受详情栏折叠状态阻断
- [x] 3.2 按用户要求不构建、不运行测试、不修改 WJY_CODE_CHANGE_LOG.md
