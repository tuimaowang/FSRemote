## 1. Static Hardware Collection

- [x] 1.1 Extend local system data structures with preferred GPU and physical-memory module details
- [x] 1.2 Implement read-only DXGI GPU summary and bounded Windows WMI memory-module/slot collection with partial-result fallback

## 2. Unified Resource Card

- [x] 2.1 Replace the two-card local page with vertical CPU, GPU, and expanded memory rows inside one card
- [x] 2.2 Render every disk with a proportional file-manager-style capacity bar and used/total/available text
- [x] 2.3 Remove local identity, operating-system, and network fields from the local page

## 3. Static Verification

- [x] 3.1 Check local-system information invariants, presentation fallbacks, and complete removal of identity/network drawing
- [x] 3.2 Record that build/test execution and the WJY change log were skipped at the user's explicit request
