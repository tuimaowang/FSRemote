## 1. 测试构建门禁

- [x] 1.1 新增默认关闭的 `FSREMOTE_BUILD_TESTS` 总开关
- [x] 1.2 让所有测试分类同时受总开关和原分类开关控制

## 2. 默认 all 目标优化

- [x] 2.1 将所有根项目测试可执行目标标记为 `EXCLUDE_FROM_ALL`
- [x] 2.2 保持 FSRemote、更新器、流 DLL 和生产静态库构建逻辑不变

## 3. 静态复核

- [x] 3.1 检查所有 `add_test` 对应的测试可执行目标均受门禁且排除出 `all`
- [x] 3.2 按用户要求不构建、不运行测试、不修改 WJY_CODE_CHANGE_LOG.md
